def new_password(oldpassword, newpassword):
    if oldpassword != newpassword and len(str(newpassword)) > 5:
        return 'True'
    else:
        return 'False'


print(new_password(1337, 'ww'))
print(new_password(1337, 'ditiseensterkwachtwoord'))
